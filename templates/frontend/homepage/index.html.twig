{% extends 'base.html.twig' %}

{% block body %}

    <section>
        <div class="jumbotron jumbotron-fluid">
            <div class="container text-center">
                <div class="row">
                    <div class="col-md-12">
                        <h1 class="display-4">Mapa útulků v ČR</h1>
                        <h3>.. přehledně a na jednom místě ..</h3>
                        <hr class="my-4" />
                        <div class="row">
                            <div class="col-md-6 offset-md-3">
                                <p class="lead">
                                    Projekt, jehož cílem je umožnit vyhledávání a vizualizaci organizací, které pomáhají zvířatům v nouzi v ČR.
                                </p>
                                <p>Veškeré informace jsou poskytovány pod svobodnou licencí Creative Commons 4.0 a může je využívat kdokoli.</p>
                            </div>
                        </div>
                        <hr class="my-4" />
                        <h2>Nyní jsme ve fázi <strong>získávání dat</strong> ...</h2>
                        <div class="row">
                            <div class="col-md-6 offset-md-3">
                                <p>
                                    Jsme na začátku našeho úsilí a snažíme se získat informace o organizacích v ČR.
                                    <strong>Pomozte nám a přidejte organizace</strong>, které znáte.
                                    Děkujeme!
                                </p>
                            </div>
                        </div>
                        <p class="lead">
                            <a class="btn btn-primary btn-lg" href="{{ path('_app_organization_add') }}" role="button">
                                Přidat útulek
                            </a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section>
        <div class="container">
            <div class="row">
                {% for category in categories %}
                    <div class="col-md-4 mb-4">
                        <div class="card h-100">
                            <div class="card-body">
                                <h2 class="card-title">Útulky {{ category.nameInAkuzativ }}</h2>
                                <a href="{{ path('_app_organizations', {'slug': category.slug}) }}" class="btn">
                                    zobrazit organizace &rarr;
                                </a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <h2 class="card-title">Všechna zvířata</h2>
                            <a href="{{ path('_app_organizations') }}" class="btn">
                                zobrazit organizace &rarr;
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock %}
